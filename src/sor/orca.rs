//! Orca Whirlpool venue adapter - stub awaiting SDK integration.
//!
//! The live query logic currently lives in [`crate::sor::router`].
//! Once the `orca-whirlpools-sdk` dependency is added to `Cargo.toml`, move
//! the `query_orca` function here, make it `pub`, and re-export it from
//! [`crate::sor`].
