//! Serum / `OpenBook` CLOB venue adapter - stub awaiting SDK integration.
//!
//! The live query logic currently lives in [`crate::sor::router`].
//! Once the `openbook-client` dependency is added to `Cargo.toml`, move the
//! `query_serum` function here, make it `pub`, and re-export it from
//! [`crate::sor`].
